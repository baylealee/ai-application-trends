name: AI Daily Digest

on:
  schedule:
    - cron: '40 0 * * *'
  workflow_dispatch:

jobs:
  build-and-send:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate daily digest placeholder
        run: |
          echo "Daily digest workflow placeholder"
          date

      - name: Build latest archive
        run: |
          echo "Future: generate JSON + daily HTML"

      - name: Send EDM placeholder
        env:
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          GAS_SUBSCRIBER_API: ${{ secrets.GAS_SUBSCRIBER_API }}
        run: |
          echo "Future: fetch subscribers from GAS"
          echo "Future: send digest EDM via Resend"
